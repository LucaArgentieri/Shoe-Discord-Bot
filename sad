bot.on('message', (msg) => {        //Elenco lista Raffle
    let rawdata;
    let data;
    if (msg.content == '!Raffle') {
            rawdata = fs.readFileSync('JsonList.json');
            console.log(rawdata);
            data = JSON.parse(rawdata);
            msg.reply(data.link);
    }
});
